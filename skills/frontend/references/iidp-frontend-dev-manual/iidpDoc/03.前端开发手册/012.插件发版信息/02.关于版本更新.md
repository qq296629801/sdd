---
title: 关于版本更新
date: 2023-11-24 10:49:26
permalink: /pages/7a6e4c/
---

## 关于版本更新

### 查看当前环境前端插件版本

**最佳推荐**：点击平台左下角工具栏按钮 `i`，在平台简介 tab页查看当前环境的前端插件版本。

>[前后端版本更新信息](http://iidp.chinasie.com:9999/iidpwiki/%E7%89%88%E6%9C%AC%E5%8F%91%E5%B8%83/%E5%89%8D%E5%90%8E%E7%AB%AF%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0%E4%BF%A1%E6%81%AF.md)
<!-- >![前后端版本更新信息（正式版）](http://iidp.chinasie.com:9999/iidpwiki/%E7%89%88%E6%9C%AC%E5%8F%91%E5%B8%83/beta%E7%89%88%E6%9C%AC.md) -->


> 为了确保线上使用的依赖版本与开发时的版本一致，有以下几项注意事项：

1. 执行`npm run update:tech`、`npm run update:beta`、`npm update`等命令升级后依赖版本中会带有 ^ 等符号，如果需要使用固定版本，需要手动修删除 ^ 符号，使用固定的具体的版本号，并执行`npm run init:tech`重新安装指定版本

```json
// 不固定的版本号，（不推荐）
{
  "dependencies": {
    "@tech/t-base": "^1.0.11",
    "@tech/t-build": "^1.0.1",
    "@tech/t-core": "^1.0.9",
    "@tech/t-el-ui": "^1.0.9",
  }
}
```


```json
// 固定(指定)的版本号
{
  "dependencies": {
    "@tech/t-base": "1.0.11",
    "@tech/t-build": "1.0.1",
    "@tech/t-core": "1.0.9",
    "@tech/t-el-ui": "1.0.9",
  }
}
```

2. 为了保证打包稳定，请直接使用当前开发环境所安装的依赖，不需要执行 ~~`npm run update:tech`~~ 或者 ~~`npm run update:beta`~~

### 版本符号对应表

| 符号   | 示例描述    |
|:------ |:---------- |
| 没有符号      | 固定版本号|
| *      | 任意版本号|
| ~      | 安装到 x.y.z 中 z 的最新版本。|
| ^      | 安装到 x.y.z 中 y 和 z 都为最新版本。 |
| >      | 接受高于指定版本的任何版本 |
| >=     | 接受等于或高于指定版本的任何版本 |
| <=     | 接受等于或低于指定版本的任何版本 |
| <     | 接受低于指定版本的任何版本 |
| =     | 接受确切的版本 |
| -     | 接受一定范围的版本，例如 2.1.0 - 2.6.2 |
| \|\|     | 组合集合，例如 < 2.1 || > 2.6 |

