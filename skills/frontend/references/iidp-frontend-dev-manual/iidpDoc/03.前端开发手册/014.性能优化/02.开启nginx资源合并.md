---
title: 开启nginx资源合并
date: 2025-04-14 11:27:45
permalink: /pages/765007/
---

## 开启nginx资源合并，减少css json请求数量

需要容器里面nginx 支持lua，可以使用 nginx -V 命令查看是否有lua字符打印出来

前端底座版本需要t-base 2.9或以上

nginx.conf
```
http {
  。。。
  lua_shared_dict concat_cache 100m;  # 在http模块添加共享内存
  。。。
  location /config {
    。。。
    add_header Allow-Concat-Resource true; # 开启资源合并配置
  }
  # 合并文件配置
	location /concatResource {
    content_by_lua_file /usr/share/nginx/html/config/lua/concat.lua;
			
    # 开启gzip
    gzip on;
    gzip_http_version 1.1;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml application/json application/javascript application/xml+rss application/atom+xml image/svg+xml;
    gzip_vary on;
    gzip_disable "MSIE [1-6]\.";
  }
  。。。
}
```