---
title: 视图查询钩子
date: 2024-05-09 15:14:53
permalink: /pages/eeae15/
---

如果是非 can 开头的钩子 返回 false 就逻辑中断

```js
test_hook_extend_view: {
    selector: {
      attr: 'id',
      value: 'menu_container_main'
    },
    type: 'merge',
    extend: 'xxxxxx',
    beforeOperate: (app, operateItem, options) => {
      return operateItem.view;
    },
    // 钩子
    hook: {
        // 标准页
        page: {
            // 查询视图钩子
            queryView: async (vm, params, options) => {
                // vm: 上下文
                // params: loadView接口参数
                console.log(' === page.queryView == params ', params);
                return await vm.super['page.queryView'](vm, params, options);
            }
        }
    }
}
```
