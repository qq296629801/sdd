---
title: 子表格钩子
date: 2024-05-28 14:35:05
permalink: /pages/aaf5d9/
---

## 上下表子表和主表单子表配置差异

```js
hook: {
  grid: { // 上下表格情况配置'grid'，主表单情况配置'form'
    ..., // 其他钩子
    tabs: {
        child_ids: { // 所属tab的field值,非Ert关系的子表配置 relateModel + unique，也可审查tab节点元素查看
            grid: { // 子表
                xxx
            }
        }
    }
  }
}
```

## 视图查询
```js
hook: {
  form: {
    tabs: {
        child_ids: {
            queryViewBefore: async (vm, params, options = {}) => {
              console.log('queryViewBefore', vm, params, options)
              return params
            },
            queryView: async (vm, params, options = {}) => {
              console.log('queryView', vm, params, options)
              return await vm.super['form.tabs.child_ids.queryView'](
                  vm,
                  params,
                  options
                )
            },
            queryViewAfter: async (vm, params, options = {}) => {
              console.log('queryViewAfter', vm, params, options)
              return params
            }
        }
    }
  }
}
```

## 是否可新增

工具栏新增按钮是否可点击

```js
hook: {
  grid: { // 上下表格情况配置'grid'，主表单情况配置'form',以下其他节点配置亦然
    ..., // 其他钩子
    tabs: {
        child_ids: { // 所属tab的field值,非Ert关系的子表配置 relateModel + unique，也可审查tab节点元素查看,以下其他节点配置亦然
            grid: { // 子表
                canCreate: async (vm, params, options) => {
                  console.log(vm.biz.grid.tabs.child_ids.grid.nodes.tbarNodes[0])
                  return true; // 返回false则按钮禁用
                };
            }
        }
    }
  }
}
```

## 是否可删除

工具栏删除按钮是否可点击

```js
hook: {
  form: {
    tabs: {
      child_ids: {
        grid: {
          // 子表
          canDelete: async (vm, params, options) => {
            // params: 选中的行数据（如果配置了勾选框则为勾选的数据，否则为蓝色背景数据）
            // options: {triggerType: checkRow/selectRow}
            if (params[0]?.id === "base.rbac.Role") {
              return false;
            }
            return true;
          };
        }
      }
    }
  }
}
```

## 是否可编辑

操作列编辑按钮是否可点击

```js
hook: {
  form: {
    tabs: {
      child_ids: {
        grid: {
          // 子表
          canEdit: async (vm, params, options) => {
            // params: 所处行数据
            if (params.rowIndex === 1) {
              return false;
            }
            return true;
          };
        }
      }
    }
  }
}
```

## 查询

表格数据查询

```js
hook: {
  form: {
    tabs: {
      child_ids: {
        grid: {
            // 查询前钩子
            beforeQuery: async (vm, params, options) => {
                // params: 调用查询接口时传递的参数
                params.testArg = 1; // 在查询接口调用前修改接口参数
                return params;
            },
            // 查询钩子
            query: async (vm, params, options) => {
                // params: 调用查询接口时传递的参数
                // 调用原有的删除逻辑
                let res = await vm.super['form.tabs.child_ids.grid.query'](vm, params, options);
                return res; // res: 查到的表格数据 {data: [{xxxx}]}
            },
            // 查询后钩子
            afterQuery: async (vm, params, options) => {
                // params: 查到的表格数据
                window.ELEMENT.Message.success('表格数据查到啦');
                return await vm.super['form.tabs.child_ids.grid.afterQuery'](vm, params, options);
            },
            // 查询总条数钩子
            queryCount: async (vm, params, options) => {
                // params: 调用查询总条数接口的参数
                return await vm.super['form.tabs.child_ids.grid.queryCount'](vm, params, options);
            },
        }
      }
    }
  }
}
```

## 删除

```js
hook: {
  form: {
    tabs: {
      child_ids: {
        grid: {
              // 删除前的二次确认弹窗
              onConfirm: async (vm, params, options) => {
                params.data.items[1].value = '确定删除？11';
                return await vm.super['form.tabs.child_ids.grid.onConfirm'](vm, params, options);
              },
              // 删除子表格数据
              delete: async (vm, params, options) => {
                console.log(' === form.tabs.child_ids.grid.delete == params ', params);
                return await vm.super['form.tabs.child_ids.grid.delete'](vm, params, options);
              }
        }
      }
    }
  }
}
```

## 选择行

表格选择行和取消选择行，点击勾选框和点击行出现蓝色背景时触发钩子

```js
hook: {
  form: {
    tabs: {
      child_ids: {
        grid: {
            // 选择行
            select: async (vm, params, options) => {
                // params: 选中的行
                return params;
            },
            // 点击勾选框取消选中行
            cancelSelect: async (vm, params, options) => {
                // params: 取消选中的行
                return params;
            }
        }
      }
    }
  }
}
select和cancelSelect钩子的options参数：{
    triggerType: 'checkbox'（点勾选框）, 'rowSelect'（点击行出现蓝色背景）
    checked,（是否选中）
    checkedData, （选中的行数据）
    paramsObj（相关数据对象）
}
```

## 工具栏和操作列按钮

**[Tech.$set 用法](/pages/e80dbe/#带super继承重新定义变量属性方法)**

```js
hook: {
  form: {
    tabs: {
      child_ids: {
        grid: {
            // 工具栏按钮钩子
            beforeToolbar: async (vm, params, options) => {
                // params: 工具栏按钮数组
                params[0].bind_on_click = (res) => {
                    console.log("重写bind_on_click事件", res);
                };
                return params;
            },
            // 使用$set改变对象属性来控制工具栏和操作列按钮
            beforeQuery: async (vm, params, options) => {
                let tbarBtn = vm.biz.grid.nodes.tbarNodes[0]; // 工具栏第一个按钮
                let operationBtn = vm.biz.grid.nodes.buttonNodes[2]; // 操作列第三个按钮
                Tech.$set(tbarBtn.data, 'options.disabled', false);
                Tech.$set(operationBtn.data, 'options.disabled', false);
                Tech.$set(
                    tbarBtn.instance,
                    'click', // bind_on_click无效 因为节点已注册
                    async (res1, res2) => {
                        try {
                            // Scope是传入的第四个参数 { a: 1 }
                            console.log(' === Scope == ', Scope, res1, res2);
                        } catch (e) {}
                        let distRes = await Super.click(params);
                    },
                    { a: 1 }
                );
                Tech.$set(
                    operationBtn.data,
                    'click', // bind_on_click无效 因为节点已注册
                    async (res1, res2) => {
                        try {
                            // Scope是传入的第四个参数 { a: 2 }
                            console.log(' === Scope == ', Scope, res1, res2);
                        } catch (e) {}
                        let distRes = await Super.click(params);
                    },
                    { a: 2 }
                );
                return params;
            }
        }
      }
    }
  }
}
```

## 行内编辑

行内编辑相关钩子，包含单行编辑和多行编辑

```js
hook: {
  form: {
    tabs: {
      child_ids: {
        grid: {
            edit: {
                    // 是否能新增行
                canCreate: async (vm, params, options) => {
                    console.log(vm.biz.form.tabs.child_ids.grid.edit.req.singleRowEditDelete)
                    return true;
                },
                    // 行内删除按钮能否点击
                canDelete: async (vm, params, options) => {
                    // params: 所处行数据
                    return true;
                },
                    // 保存按钮能否点击 包括单行和多行的保存按钮
                canSave: async (vm, params, options) => {
                    // params: 单行编辑时为所处行数据
                    return true;
                },
                    // 单行编辑行内删除前的二次确认弹窗
                onConfirm: async (vm, params, options) => {
                    // params：要删除的id数组
                    window.Tech.confirmbox({
                    width: '45%',
                    title: '请注意',
                    items: [
                        {
                        id: vm.$ds.idPre + 'deleteRow_confirmBox',
                        value: '确定要删除此数据吗',
                        type: 'text'
                        }
                    ],
                    options: {
                        confirm: async (close) => {
                        options.deleteFn(close);
                        close();
                        },
                        cancle: (close) => {
                        close();
                        }
                    }
                    });
                    return params;
                },
                    // 单行编辑删除前
                beforeSingleRowDelete: async (vm, params, options) => {
                    // params：要删除的id数组
                    return params;
                },
                    // 单行编辑删除
                singleRowDelete: async (vm, params, options) => {
                    // params：要删除的id数组
                    return vm.super['form.tabs.child_ids.grid.edit.singleRowDelete'](vm, params, options);
                },
                    // 单行编辑删除后
                afterSingleRowDelete: async (vm, params, options) => {
                    return vm.super['form.tabs.child_ids.grid.edit.afterSingleRowDelete'](vm, params, options);
                },
                    // 保存前 包括单行和多行的保存
                beforeSave: async (vm, params, options) => {
                    // params：要保存的数据
                    return params; // 返回调用接口时保存的数据
                },
                    // 保存 包括单行和多行的保存
                save: async (vm, params, options) => {
                    // params：要保存的数据
                    // options.triggerType: multipleRow / singleRow
                    return await vm.super['form.tabs.child_ids.grid.edit.save'](vm, params, options);
                },
                    // 保存后 包括单行和多行的保存
                afterSave: async (vm, params, options) => {
                    return await vm.super['form.tabs.child_ids.grid.edit.afterSave'](vm, params, options);
                }
            }
        }
      }
    }
  }
}
```

## 子表搜索栏

子表搜索栏钩子

```js
hook: {
  form: {
    tabs: {
      child_ids: {
        grid: {xxx},
        search: {
            // 是否能搜索
            canQuery: async (vm, params, options) => {
                return true; // 返回false则搜索按钮不能点击
            },
            // 搜索校验
            validateQuery: async (vm, params, options) => {
                return await vm.super['form.tabs.child_ids.search.validateQuery'](vm, params, options);
            }
        }
      }
    }
  }
}
```

## 抽屉表单

点击子表编辑按钮或双击行弹出的抽屉的钩子

```js
hook: {
  form: {
    tabs: {
      child_ids: {
        grid: {xxx},
        search: {xxx},
        drawerForm: {
            // 查询视图
            queryView: async (vm, params, options) => {
              return await vm.super['form.tabs.child_ids.drawerForm.queryView'](vm, params, options);
            },
            // 抽屉表单数据查询前
            beforeQuery: async (vm, params, options) => {
              return params;
            },
            // 抽屉表单数据查询
            query: async (vm, params, options) => {
              return await vm.super['form.tabs.child_ids.drawerForm.query'](vm, params, options);
            },
            // 抽屉表单数据查询后
            afterQuery: async (vm, params, options) => {
              return await vm.super['form.tabs.child_ids.drawerForm.afterQuery'](
                vm,
                params,
                options
              );
            },
            // 抽屉表单初始化
            init: async (vm, params, options) => {
              return params;
            },
            // 抽屉表单校验钩子
            validate: async (vm, params, options) => {
              return params
            },
            // 抽屉表单保存前钩子
            beforeSave: async (vm, params, options) => {
              return params
            },
            // 抽屉表单保存钩子
            save: async (vm, params, options) => {
              return await vm.super['form.tabs.child_ids.drawerForm.save'](vm, params, options);
            },
            // 抽屉表单保存后钩子
            afterSave: async (vm, params, options) => {
              return params
            }
        }
      }
    }
  }
}
```

## 添加关联

点击子表工具栏添加按钮弹出的添加关联弹窗的钩子

```js
hook: {
  form: {
    tabs: {
      child_ids: {
        grid: {xxx},
        search: {xxx},
        drawerForm: {xxx},
        addRel: {
              // 查询视图
              queryView: async (vm, params, options) => {
                return await vm.super['form.tabs.child_ids.addRel.queryView'](vm, params, options);
              },
              beforeQuery: async (vm, params, options) => {
                return params;
              },
              query: async (vm, params, options) => {
                return await vm.super['form.tabs.child_ids.addRel.query'](vm, params, options);
              },
              afterQuery: async (vm, params, options) => {
                return await vm.super['form.tabs.child_ids.addRel.afterQuery'](vm, params, options);
              },
              queryCount: async (vm, params, options) => {
                return await vm.super['form.tabs.child_ids.addRel.queryCount'](vm, params, options);
              },
              save: async (vm, params, options) => {
                return await vm.super['form.tabs.child_ids.addRel.save'](vm, params, options);
              }
        }
      }
    }
  }
}
```
